#!/usr/bin/env python3
"""WorkLoom 熊猫电商运营系统 · 销售 PPT 生成器（16 页，70% 业务价值 + 30% 技术体验，含演讲备注）"""
from pptx import Presentation
from pptx.util import Inches, Pt, Emu
from pptx.dml.color import RGBColor
from pptx.enum.text import PP_ALIGN, MSO_ANCHOR

NAVY = RGBColor(0x1B, 0x2A, 0x4E)
NAVY2 = RGBColor(0x24, 0x36, 0x5F)
GOLD = RGBColor(0xC9, 0xA4, 0x5C)
WHITE = RGBColor(0xFF, 0xFF, 0xFF)
INK = RGBColor(0x1C, 0x23, 0x33)
SUB = RGBColor(0x6B, 0x76, 0x90)
GREEN = RGBColor(0x2E, 0x9E, 0x6B)
RED = RGBColor(0xD6, 0x45, 0x45)
AMBER = RGBColor(0xD9, 0x93, 0x0D)
BG = RGBColor(0xF4, 0xF6, 0xFA)
LINE = RGBColor(0xE6, 0xEA, 0xF2)

prs = Presentation()
prs.slide_width = Inches(13.333)
prs.slide_height = Inches(7.5)
BLANK = prs.slide_layouts[6]
SW, SH = 13.333, 7.5

def slide():
    return prs.slides.add_slide(BLANK)

def rect(s, x, y, w, h, color, line=False):
    from pptx.enum.shapes import MSO_SHAPE
    sh = s.shapes.add_shape(MSO_SHAPE.RECTANGLE, Inches(x), Inches(y), Inches(w), Inches(h))
    sh.fill.solid(); sh.fill.fore_color.rgb = color
    if not line: sh.line.fill.background()
    else: sh.line.color.rgb = LINE; sh.line.width = Pt(0.75)
    sh.shadow.inherit = False
    return sh

def rrect(s, x, y, w, h, color):
    from pptx.enum.shapes import MSO_SHAPE
    sh = s.shapes.add_shape(MSO_SHAPE.ROUNDED_RECTANGLE, Inches(x), Inches(y), Inches(w), Inches(h))
    sh.adjustments[0] = 0.08
    sh.fill.solid(); sh.fill.fore_color.rgb = color
    sh.line.color.rgb = LINE; sh.line.width = Pt(0.75)
    sh.shadow.inherit = False
    return sh

def text(s, x, y, w, h, runs, size=14, color=INK, bold=False, align=PP_ALIGN.LEFT, anchor=MSO_ANCHOR.TOP, leading=1.15):
    tb = s.shapes.add_textbox(Inches(x), Inches(y), Inches(w), Inches(h))
    tf = tb.text_frame; tf.word_wrap = True; tf.vertical_anchor = anchor
    if isinstance(runs, str): runs = [runs]
    for i, line in enumerate(runs):
        p = tf.paragraphs[0] if i == 0 else tf.add_paragraph()
        p.alignment = align; p.line_spacing = leading
        if isinstance(line, tuple):
            txt, kw = line
        else:
            txt, kw = line, {}
        r = p.add_run(); r.text = txt
        f = r.font
        f.size = Pt(kw.get("size", size)); f.bold = kw.get("bold", bold)
        f.color.rgb = kw.get("color", color); f.name = "PingFang SC"
        p.space_after = Pt(kw.get("space", 4))
    return tb

def notes(s, txt):
    s.notes_slide.notes_text_frame.text = txt

def header(s, title, sub=""):
    rect(s, 0, 0, SW, 0.16, GOLD)
    text(s, 0.6, 0.42, 12, 0.7, title, size=26, bold=True, color=NAVY)
    if sub:
        text(s, 0.6, 1.08, 12.2, 0.4, sub, size=13, color=SUB)

def kpi_card(s, x, y, w, h, label, value, delta, vc=NAVY):
    rrect(s, x, y, w, h, WHITE)
    text(s, x+0.2, y+0.14, w-0.4, 0.3, label, size=11.5, color=SUB)
    text(s, x+0.2, y+0.44, w-0.4, 0.55, value, size=25, bold=True, color=vc)
    text(s, x+0.2, y+1.02, w-0.4, 0.3, delta, size=10.5, color=GREEN if "▲" in delta or "省" in delta or "✓" in delta else SUB)

def bullet_card(s, x, y, w, h, title, items, tc=NAVY):
    rrect(s, x, y, w, h, WHITE)
    text(s, x+0.22, y+0.14, w-0.44, 0.35, title, size=14, bold=True, color=tc)
    lines = [("· " + it, {"size": 11.5, "space": 5}) for it in items]
    text(s, x+0.22, y+0.56, w-0.44, h-0.7, lines, size=11.5, color=INK)

def bar_row(s, x, y, w, label, pct, val, color=GOLD, lw=1.7):
    text(s, x, y, lw, 0.3, label, size=11, color=SUB)
    rect(s, x+lw, y+0.05, (w-lw-1.0), 0.2, RGBColor(0xED, 0xF0, 0xF6))
    rect(s, x+lw, y+0.05, (w-lw-1.0)*pct/100.0, 0.2, color)
    text(s, x+w-0.95, y-0.02, 0.95, 0.3, val, size=11, bold=True, color=INK, align=PP_ALIGN.RIGHT)

# ============ S1 封面 ============
s = slide()
rect(s, 0, 0, SW, SH, NAVY)
rect(s, 0, SH-0.5, SW, 0.5, GOLD)
text(s, 1.0, 1.5, 11.3, 1.0, "WorkLoom 熊猫电商运营系统", size=44, bold=True, color=WHITE)
text(s, 1.0, 2.55, 11.3, 0.6, "电商经营样板间 · 让每一位卖家，在签约之前先进自己未来的运营中心", size=18, color=GOLD)
for i, (t, d) in enumerate([("夜班班组","跨时区盯盘 0 漏接"),("利润保险丝","ACoS 爆表自动刹车"),("一人多店","管理半径 3→15 家"),("门道内置","48 ★技能固化赚钱经验")]):
    rrect(s, 1.0+i*2.95, 4.1, 2.7, 1.15, NAVY2)
    text(s, 1.2+i*2.95, 4.28, 2.3, 0.4, t, size=15, bold=True, color=GOLD)
    text(s, 1.2+i*2.95, 4.7, 2.3, 0.4, d, size=12, color=WHITE)
text(s, 1.0, 6.1, 11, 0.4, "腰部卖家 · 国内大卖 · 跨境集团 ｜ 一份 1:1 可交互的经营样板间", size=14, color=RGBColor(0xC8,0xD4,0xEE))
notes(s, "开场 30 秒：各位都是电商行家，我先说结论——这套系统能让腰部卖家一年多留 112 到 347 万，夜班不用人，一个人能管十几家店。今天我不讲 PPT 概念，我们带了一个 1:1 的『经营样板间』，所有数字都可以现场点开验证。数据均为模拟数据，实际可替换。")

# ============ S2 痛点 ============
s = slide()
header(s, "您的店正在漏钱，而且漏得无声无息", "多平台电商经营的行业性困境（调研：腰部卖家/国内大卖/跨境集团一线作业）")
kpi_card(s, 0.6, 1.6, 2.95, 1.5, "八大流失点", "8–15%", "占年营收（≈一个运营小组）", RED)
kpi_card(s, 3.75, 1.6, 2.95, 1.5, "运营机械劳动", "40%+", "跨系统人工操作占工时", AMBER)
kpi_card(s, 6.9, 1.6, 2.95, 1.5, "夜班与客服", "刚需", "欧美白天=中国深夜", RED)
kpi_card(s, 10.05, 1.6, 2.7, 1.5, "员工流失率", "30–50%", "经验随人走", AMBER)
bullet_card(s, 0.6, 3.4, 5.95, 3.5, "八大流失点（年营收 3000 万腰部卖家测算）", [
    "客服响应慢丢单 —— 售前 3 分钟不答转化腰斩",
    "广告没保险丝 —— ACoS 爆表还在加预算",
    "差评响应慢 —— 隔夜才看到差评",
    "断货与压货 —— 库存没当现金管",
    "价格倒挂 —— 多平台价差被羊毛党狙击",
    "对账差异/老客流失/内容陈旧 —— 长尾失血",
])
bullet_card(s, 6.8, 3.4, 5.95, 3.5, "三个客群，三种最痛", [
    "腰部卖家：一人多岗救火队员，没时间经营",
    "国内大卖：各平台数据孤岛，大促全员救火",
    "跨境集团：欧美白天=中国深夜，没人盯盘",
    "",
    "共同根因：机械重复占满时间，",
    "真正需要判断的工作被挤占。",
])
notes(s, "这页只讲客户自己的痛。引导话术：『客服响应慢这一项，是转化最大的漏斗——各位可以回忆一下，昨天有多少条询盘超过 3 分钟才回？』数字来源：行业调研+一线作业调研，均为模拟测算口径，实际可替换。")

# ============ S3 解决方案总览 ============
s = slide()
header(s, "解决方案：不是「AI 帮你干活」，是「活儿的干法变了」", "一套经营操作系统 + 一支 24 小时数字员工军团，人类只做最值钱的判断")
for i, (t, d) in enumerate([
    ("夜班班组", "22:00 接棒：竞对采集→多平台对账→广告巡检→08:30 决策包。跨时区盯盘零漏接。"),
    ("利润保险丝", "ACoS 连续 3 天超盈亏线自动降预算 30% 并请示。老板定边界，系统做执行。"),
    ("多平台看门狗", "价格倒挂预警、断货防御、13 平台账单勾稽，对账只剩差异项。"),
    ("口碑防御", "差评 5 分钟检出+2h SLA 升级；多模态客服看懂截图/图片/视频。"),
    ("一人多店", "店铺矩阵看板一屏管多店：GMV 同屏、决策包汇聚、告警收敛。"),
    ("技能市场", "160 个官方技能是可见资产：夜班/定价/对账/客服/直播/合规…一键加装。"),
]):
    x, y = 0.6 + (i % 3) * 4.22, 1.65 + (i // 3) * 2.55
    bullet_card(s, x, y, 3.95, 2.35, t, [d[:52] + "…" if len(d) > 52 else d for d in [d]], GOLD if i % 2 == 0 else NAVY)
text(s, 0.6, 6.85, 12, 0.4, "三种客群装配（腰部/大卖/跨境集团）一键切换，同一底座，不同编制。", size=13, color=SUB)
notes(s, "总览页只讲结构：一套底座、六大能力域、三档客群装配。强调『不是 6 个软件，是一个系统』。后面逐条展开。")

# ============ S4 范式重构 ============
s = slide()
header(s, "工作范式重构：原来的事，很多已经不存在了", "回到每件事的目的，用全新方式达成——这是增量，不是效率")
rows = [
    ("对账", "每月几天逐平台导表比对", "账单×订单×广告三方勾稽，对账动作消失"),
    ("客服排班", "三班倒还漏消息", "多模态客服 7×24，人工只剩例外通道"),
    ("手动调价", "每天凭感觉改几个价", "价格是围栏内连续函数，老板只做策略家"),
    ("做报表", "月底半天 Excel 汇总", "自然语言提问，秒级出数"),
    ("盯广告", "早中晚三次看 ACoS", "利润保险丝自动熔断，只看不报错的"),
    ("差评救火", "隔夜看到再补救", "5 分钟检出 + 2 小时 SLA 防御窗口"),
]
y = 1.7
text(s, 0.6, y-0.1, 3.4, 0.35, "原来的事", size=13, bold=True, color=SUB)
text(s, 4.2, y-0.1, 4.3, 0.35, "过去怎么做", size=13, bold=True, color=SUB)
text(s, 8.7, y-0.1, 4.1, 0.35, "现在（范式重构后）", size=13, bold=True, color=GOLD)
y += 0.35
for name, old, new in rows:
    rrect(s, 0.6, y, 12.15, 0.68, WHITE)
    text(s, 0.8, y+0.12, 3.2, 0.4, name, size=14, bold=True, color=NAVY)
    text(s, 4.2, y+0.14, 4.3, 0.4, old, size=12, color=SUB)
    text(s, 8.7, y+0.14, 4.0, 0.4, new, size=12, bold=True, color=GREEN)
    y += 0.8
text(s, 0.6, y+0.05, 12, 0.4, "不是同一件事做得更快——是这件事的目的被用新方式达成，原工作本身消失。", size=13, color=SUB)
notes(s, "这是全场最重要的一页。逐行讲，每行一个问题引导：『上个月对账财务花了几天？』『客服排班表上个月的排班您还记得吗？』让客户自己意识到旧范式的荒谬，再给新范式。")

# ============ S5 价值量化：降本 ============
s = slide()
header(s, "业务价值 ①：降本 —— 操作岗自动化，年化数百万", "腰部卖家（年营收 3000 万）测算（模拟数据，实际可替换）")
items = [("客服应答", 95), ("多平台对账", 88), ("刊登与改价", 80), ("广告巡检", 75), ("报表生成", 70), ("评价回复", 60)]
y = 1.75
for label, mins in items:
    pct = int(mins / 120 * 100)
    bar_row(s, 0.6, y, 7.2, label, pct, f"{mins}→{int(mins*0.15)}分钟", GOLD, 1.9)
    y += 0.62
bullet_card(s, 8.15, 1.7, 4.6, 3.6, "降本汇总（年化）", [
    "操作岗人力大幅下降",
    "客服+对账两岗先行自动化",
    "其中夜班客服一项：",
    "替代两班倒坐席",
    "—— 仅此一项即覆盖全年订阅",
])
kpi_card(s, 8.15, 5.5, 4.6, 1.35, "保守情景价值 / 订阅费", "≥ 3 倍", "最悲观假设组合下仍成立", GREEN)
notes(s, "降本页讲保守逻辑：所有数字取下限，夜班客服单独算账——『您雇一个夜班坐席一年多少钱，系统订阅费比这个低，这一项就回本，其他全是净赚。』")

# ============ S6 价值量化：增收 ============
s = slide()
header(s, "业务价值 ②：增收 —— 堵住八大流失点", "腰部卖家年化 112–347 万；跨境集团 3.6–14.9 亿（模拟数据，实际可替换）")
items2 = [("客服秒回（多模态+夜班）", 70, "转化↑"), ("广告保险丝（R3 熔断）", 80, "ACoS↓"), ("差评 2h SLA（R9）", 90, "权重↑"), ("断货防御（R7 预警）", 85, "GMV↑"), ("价格守护（倒挂预警）", 95, "毛利↑")]
y = 1.75
for label, pct, val in items2:
    bar_row(s, 0.6, y, 7.6, label, pct, val, GREEN, 3.1)
    y += 0.66
kpi_card(s, 8.55, 1.75, 4.2, 1.5, "腰部卖家（中性）", "112–347万/年", "降本 + 增收合并测算", NAVY)
kpi_card(s, 8.55, 3.45, 4.2, 1.5, "国内大卖（管理半径）", "5→15家", "矩阵看板+中台共享", NAVY)
kpi_card(s, 8.55, 5.15, 4.2, 1.5, "跨境集团（年化价值）", "3.6–14.9亿", "夜班盯盘 0 漏接", GREEN)
notes(s, "增收页强调『流失是可以堵的』：每个流失点对应一个具体机制，不是口号。右下两个卡给大卖和跨境客户各一个钩子。")

# ============ S7 夜班 ============
s = slide()
header(s, "核心场景 ①：夜班班组 —— 您睡后，店里在发生什么", "跨时区经营（欧美白天=中国深夜）的系统解法")
steps = [("18:00","候选清单","3 项任务+预估积分+命中围栏摘要，您一键开启"),("22:00","围栏快照 v3","当晚所有动作 100% 过围栏，版本留痕"),("23:00","竞对采集","竞店竞品价格卡自动入库"),("00:00","多平台对账","账单×订单×广告三方勾稽，差异当日现"),("02:00","广告巡检","ACoS 异常自动降权，保险丝待命"),("05:00","欧美客服高峰","多语言客服中心满编应答"),("08:30","决策包","✓已完成 / ◆待审批 / ▲需介入 推送到您手机")]
y = 1.75
for t, h, d in steps:
    rrect(s, 0.6, y, 1.3, 0.58, NAVY)
    text(s, 0.6, y+0.1, 1.3, 0.4, t, size=13, bold=True, color=GOLD, align=PP_ALIGN.CENTER)
    rrect(s, 2.05, y, 10.7, 0.58, WHITE)
    text(s, 2.25, y+0.05, 3.2, 0.4, h, size=12.5, bold=True, color=NAVY)
    text(s, 5.5, y+0.09, 7.1, 0.4, d, size=11.5, color=SUB)
    y += 0.7
text(s, 0.6, y+0.02, 12.2, 0.5, "任意时刻一键暂停 ≤60 秒全端生效 · 断点续跑 · 动作全留痕可复盘。跨境版叙事：把夜晚还给老板。", size=13, bold=True, color=GOLD)
notes(s, "按时间线讲一个完整的夜。落点：『过去这 8 小时需要两班倒的坐席和盯盘运营，现在是一个班组，而且每个动作都可以复盘——人类夜班从来做不到 100% 留痕。』")

# ============ S8 一人多店 ============
s = slide()
header(s, "核心场景 ②：一人多店 —— 店铺矩阵看板", "管理半径从 3 家店到 15 家店")
kpi_card(s, 0.6, 1.7, 3.9, 1.45, "熊猫优选天猫店（3C配件）", "日销 ¥768万", "✓9 ◆1 ▲0 · ACoS 达标↓", NAVY)
kpi_card(s, 4.72, 1.7, 3.9, 1.45, "熊猫严选抖音店（直播）", "日销 ¥438万", "✓6 ◆2 ▲0 · 差评 SLA 100%", NAVY)
kpi_card(s, 8.84, 1.7, 3.9, 1.45, "PandaTech 亚马逊（美欧日）", "日销 $580万", "✓8 ◆0 ▲1 · 跟卖已驱赶↓", NAVY)
bullet_card(s, 0.6, 3.45, 5.95, 3.4, "一屏管所有店", [
    "14 店 GMV/毛利/ACoS/目标达成 同屏对比",
    "各店夜班决策包汇聚（三栏合并视图）",
    "跨店待审批收件箱合并，手机 30 秒批完",
    "告警收敛：同源合并，只推该看的那条",
    "价格健康跨店排名：倒挂风险一目了然",
])
bullet_card(s, 6.8, 3.45, 5.95, 3.4, "三种客群一套系统", [
    "腰部卖家：客服+对账两岗先行",
    "国内大卖：作战军满编+数据中台",
    "跨境集团：两军+5 中台+多语言客服中心",
    "客群围栏 patch 自动叠加（只紧不松）",
    "—— 不需要为每个平台买一套软件",
])
notes(s, "多店老板最懂这页。话术：『您现在管 5 家店，每天进 5 个后台。用矩阵看板，每天 5 分钟看完全部——多出来的精力，足够再开 5 家。』")

# ============ S9 目标追踪 ============
s = slide()
header(s, "核心场景 ③：经营目标 —— 从年初口号到每周作战", "目标设定 → 自动分解 → 实时追踪 → 偏差归因 → 联动补救")
for i, (t, d) in enumerate([("设定","年度目标录入，系统按历史曲线建议分解值"),("分解","自动拆到月/平台/SKU"),("追踪","仪表盘实时比对目标 vs 时序进度"),("归因","偏差自动下钻：竞对降价 ×2 + 断货 ×3"),("联动","经营分析参谋自动生成补救方案（审批级）")]):
    x = 0.6 + i * 2.52
    rrect(s, x, 1.8, 2.3, 1.7, WHITE)
    text(s, x+0.15, 1.95, 2.0, 0.4, f"{i+1} · {t}", size=14, bold=True, color=GOLD)
    text(s, x+0.15, 2.4, 2.05, 1.0, d, size=11, color=INK)
kpi_card(s, 0.6, 4.0, 2.9, 1.4, "GMV 目标达成", "78%", "略落后时序 · 已预警", AMBER)
kpi_card(s, 3.7, 4.0, 2.9, 1.4, "月营收达成", "96%", "¥2,480万 / ¥2,580万", NAVY)
kpi_card(s, 6.8, 4.0, 2.9, 1.4, "差评率", "1.6%", "目标 ≤2% · ✓ 达标", GREEN)
kpi_card(s, 9.9, 4.0, 2.85, 1.4, "复购率", "28.4%", "目标 28% · ✓ 达标", GREEN)
text(s, 0.6, 5.75, 12.2, 0.9, "过去：目标写在 Excel 里，年底才想起来。现在：每周一早上，目标仪表盘和偏差归因就在您手机上，落后会自动给出补救方案。", size=14, color=SUB)
notes(s, "这页回应『目标设定与追踪』。关键点：目标不是报表，是作战系统——落后了系统会告诉您为什么、怎么办。")

# ============ S10 技术体验 ① 围栏 ============
s = slide()
header(s, "技术体验 ①：安全不是承诺，是物理约束", "围栏引擎 R1–R30：AI 的能力边界写在系统里（技术占 30%，落到客户收益）")
bullet_card(s, 0.6, 1.7, 5.95, 3.1, "三级判定（一切动作 100% 过围栏）", [
    "auto：常规动作直接执行，全程留痕",
    "review：涉钱/涉买家/涉对外 → 挂起等您 30 秒审批",
    "block：高危动作物理熔断+告警",
    "—— 拒绝默认：数据缺失时熔断，不猜测",
    "—— deny 优先：多规则命中取最严",
])
bullet_card(s, 6.8, 1.7, 5.95, 3.1, "基线 30 条，只紧不松", [
    "R2 毛利红线熔断：破红线系统物理无法执行",
    "R5 大额退款必审 · R10 涉费承诺必审",
    "R11 违规禁区：刷单/删差评/侵权词只阻断",
    "R16/R17 夜班白名单：夜间调价 ≤3%",
    "R18 价格倒挂守护（多平台钱袋子）",
    "任何自定义只可收紧：放宽提案系统当面拒绝",
])
kpi_card(s, 0.6, 5.1, 3.9, 1.4, "30 天围栏判定", "38,412 次", "100% 动作过围栏 · 零越线", NAVY)
kpi_card(s, 4.72, 5.1, 3.9, 1.4, "熔断拦截", "17 次", "倒挂 5 · 违规 3 · 高危 9", RED)
kpi_card(s, 8.84, 5.1, 3.9, 1.4, "因失控造成的直接损失", "¥0", "（模拟数据，实际可替换）", GREEN)
notes(s, "客户最怕 AI 乱来，这页正面回应。『您不需要信任 AI，您只需要验证围栏——破毛利红线这件事，系统物理上做不出来，这不是提醒，是拒绝。』现场可演示单调守卫驳回放宽提案。")

# ============ S11 技术体验 ② 移动+多模式+排班+夜班员工管理 ============
s = slide()
header(s, "技术体验 ②：人性化设计，每一项都落到您的收益", "移动端 · 多客群适配 · 夜班班组管理 · 目标追踪")
bullet_card(s, 0.6, 1.7, 5.95, 2.4, "📱 店铺装进口袋（移动端 App）", [
    "决策包/三手势审批/多店切换/一键制动 全部手机完成",
    "人在出差、看厂、度假，管理不中断",
    "收益：管理不被物理位置绑架，多店才可能",
])
bullet_card(s, 6.8, 1.7, 5.95, 2.4, "🌙 夜班班组管理：从管人到管班组", [
    "每晚班次全留痕：候选/快照/动作/暂停/交付",
    "人类只处理 ▲需介入（平均 0.3 次/晚）",
    "收益：1 个白班兼管夜班，出错 100% 可复盘",
])
bullet_card(s, 0.6, 4.3, 5.95, 2.4, "🏬 多客群装配（腰部/大卖/跨境）", [
    "客群一键切换，编制模板自动装配",
    "跨境：多语言客服中心旗舰位，老板夜间可关机",
    "收益：一套系统开三种盘，扩张零边际系统成本",
])
bullet_card(s, 6.8, 4.3, 5.95, 2.4, "🎯 目标设定与追踪", [
    "目标自动分解到月/平台/SKU，落后自动归因",
    "补救方案自动生成（审批级），闭环到行动",
    "收益：经营目标达成率从凭感觉变可管理",
])
notes(s, "30% 技术体验页，但每一条都要落回收益。讲法：左边是『你看到了什么』，右边是『这给你省了/赚了什么』。")

# ============ S12 场景案例：一天 ============
s = slide()
header(s, "场景化案例：王老板的一天（熊猫优选集团 · 样板间卖家）", "虚构样板间卖家 · 模拟数据，实际可替换")
rows = [
    ("08:30", "看夜班决策包（3 分钟）", "✓9 项已完成 · ◆1 项待审 · ▲0 需介入", "过去：1 小时翻 5 个后台"),
    ("08:45", "三手势批差评回复（30 秒）", "AI 草稿+补偿方案，采纳即发布", "过去：下午才看到差评"),
    ("10:00", "晨会 5 分钟", "只讨论决策包例外项", "过去：30 分钟信息通报"),
    ("14:00", "大促上新", "刊登工厂多平台一键搬家，零排队", "过去：3 天逐平台上架"),
    ("16:30", "手机收到熔断提醒", "拼多多价格倒挂被 R18 拦截，损失 ¥0", "过去：月底对账才发现"),
    ("20:00", "确认明日投放（10 分钟）", "预算大盘建议，一键确认", "过去：每周 2 小时拼表"),
    ("22:00", "夜班班组接棒，老板下班", "围栏快照 v3，全自治", "过去：夜班坐席两班倒"),
]
y = 1.7
for t, a, r, old in rows:
    rrect(s, 0.6, y, 1.2, 0.62, NAVY)
    text(s, 0.6, y+0.11, 1.2, 0.4, t, size=13, bold=True, color=GOLD, align=PP_ALIGN.CENTER)
    rrect(s, 1.95, y, 6.3, 0.62, WHITE)
    text(s, 2.15, y+0.05, 6.0, 0.55, [(a, {"size": 12, "bold": True, "space": 1}), (r, {"size": 10.5, "color": SUB})])
    rrect(s, 8.4, y, 4.35, 0.62, RGBColor(0xFB, 0xF1, 0xDA))
    text(s, 8.55, y+0.16, 4.1, 0.4, old, size=11, color=AMBER)
    y += 0.74
notes(s, "用一天的时间线把前面所有能力串起来。这是客户最容易代入的一页——每项后面都标了『过去』做对比。讲完后自然过渡到：这样的生活，下周就可以是您的。")

# ============ S13 样板间策略 ============
s = slide()
header(s, "为什么您可以相信：样板间策略", "楼还在盖，但您的运营中心已经装好了——1:1 还原，现场可验证")
bullet_card(s, 0.6, 1.7, 5.95, 3.0, "什么是经营样板间", [
    "功能/流程/交互：1:1 真实，与正式系统一致",
    "业务数据：30 天模拟经营态（数字孪生）",
    "外部接口（平台 API/ERP）：工程确定性工作",
    "—— 如同售楼处样板间：装修 1:1，水电可开",
])
bullet_card(s, 6.8, 1.7, 5.95, 3.0, "今天就能验证的", [
    "30 天夜班战报：每晚决策包真实可点开",
    "全量事件哈希链：现场逐条重算验证",
    "熔断实录：倒挂/违规/高危拦截全留痕",
    "—— 包括系统自己的操作也不可篡改",
])
kpi_card(s, 0.6, 5.0, 3.9, 1.45, "数字孪生", "30 天", "14 店铺 × 13 平台全业务域", NAVY)
kpi_card(s, 4.72, 5.0, 3.9, 1.45, "样板间管家", "5 分钟", "实时补数 · 演示永不冷场", NAVY)
kpi_card(s, 8.84, 5.0, 3.9, 1.45, "质量门禁", "全绿", "全场景用例套件实测通过", GREEN)
notes(s, "这页把『模拟数据』从弱点变成卖点：我们不是给您看 PPT，是给您看 1:1 的运营中心。现场打开原型，点给客户看——这是整套销售流程的杀手锏。")

# ============ S14 三客群 ============
s = slide()
header(s, "您属于哪种卖家，就听哪种价值", "同一底座，三套叙事")
bullet_card(s, 0.6, 1.7, 3.95, 4.6, "🏬 腰部卖家 · 卖 ROI", [
    "一年多留 112–347 万",
    "夜班客服一项即覆盖订阅",
    "客服/广告/差评/断货 流失全堵",
    "目标追踪：每周作战而非年底复盘",
    "—— 适合单平台多店（年营收约 3000 万）",
])
bullet_card(s, 4.72, 1.7, 3.95, 4.6, "🐼 国内大卖 · 卖规模", [
    "一个人管 15 家店",
    "数据中台：13 平台一口径",
    "大促作战室：战备而不乱",
    "SKU 损益账本：每块钱可归因",
    "—— 适合多平台全线作战的大卖",
])
bullet_card(s, 8.84, 1.7, 3.95, 4.6, "🌏 跨境集团 · 卖时区", [
    "欧美白天=中国深夜，军团盯盘",
    "多语言客服中心 8 语种 24h",
    "VAT/关税/汇率 合规中台",
    "围栏+哈希链：敢托付深夜的自治",
    "—— 适合跨境多平台超级大卖集团",
])
notes(s, "按在座客户构成分配时间。腰部卖家多就重点讲 ROI，跨境多就讲『关机睡觉』——这一句在跨境老板群体里的杀伤力极大。")

# ============ S15 实施与保障 ============
s = slide()
header(s, "落地路径：下周就能用上", "实施五步 + 三条保障")
for i, (t, d) in enumerate([("第 1 天 · 授权建档","平台店铺授权+一店一档 30+ 字段组建档（毛利红线/审批矩阵/合规禁区），围栏基线 R1–R30 即刻生效"),("第 1 周 · 上线","一盘货导入+客服与对账两岗先行上线，当晚开始产出决策包"),("第 2–4 周 · 深化","编制按客群模板扩编，组织记忆开始沉淀，门道技能持续加装")]):
    bullet_card(s, 0.6 + i*4.22, 1.7, 3.95, 2.1, t, [d], NAVY)
bullet_card(s, 0.6, 4.1, 5.95, 2.6, "三条保障", [
    "安全：围栏物理约束+哈希链可验证，不是口头承诺",
    "经验：您店的 know-how 沉淀为组织资产，人走茶不凉",
    "演进：技能市场持续上架，系统越用越值（续费即复利）",
])
bullet_card(s, 6.8, 4.1, 5.95, 2.6, "我们不做什么（边界诚实）", [
    "不做 ERP：与聚水潭们共生，做 ERP 之上的决策层",
    "不碰违规动作：刷单/删差评 AI 物理阻断",
    "不用模拟数据冒充真实案例：样板间数据均明确标注",
])
notes(s, "落地页给客户确定性：第 1 天就有价值（围栏生效），第 1 周就有体感（决策包）。『我们不做什么』这栏反而增强信任。")

# ============ S16 CTA ============
s = slide()
rect(s, 0, 0, SW, SH, NAVY)
rect(s, 0, 0, SW, 0.16, GOLD)
text(s, 1.0, 0.9, 11.3, 0.8, "现在，走进您的运营中心", size=36, bold=True, color=WHITE)
text(s, 1.0, 1.85, 11.3, 0.5, "30 分钟现场演示：您点，我们看——每一个数字都可以下钻验证", size=17, color=GOLD)
for i, (t, d) in enumerate([("① 看昨夜", "打开夜班决策包：✓9 ◆1 ▲0，点开任何一项看原始留痕"), ("② 点审批", "亲手完成一次三手势审批：30 秒，从快照到决断"), ("③ 现场熔断", "我们当场制造一次价格倒挂——看 R18 如何拦截"), ("④ 问数据", "您随口问一个经营问题，系统秒级出数")]):
    rrect(s, 1.0 + (i % 2) * 5.85, 2.75 + (i // 2) * 1.45, 5.6, 1.25, NAVY2)
    text(s, 1.25 + (i % 2) * 5.85, 2.92 + (i // 2) * 1.45, 5.1, 0.4, t, size=16, bold=True, color=GOLD)
    text(s, 1.25 + (i % 2) * 5.85, 3.36 + (i // 2) * 1.45, 5.15, 0.6, d, size=12, color=WHITE)
text(s, 1.0, 5.9, 11.3, 0.4, "WorkLoom 熊猫电商运营系统 ｜ 预约 30 分钟样板间演示 ｜ 模拟数据，实际可替换", size=14, color=RGBColor(0xC8, 0xD4, 0xEE))
notes(s, "收尾只推一个动作：预约 30 分钟现场演示。不要卖系统，卖『进店看看』。现场演示四步曲就是右侧这四步——客户亲手点过，成交率完全不同。")

prs.save("/workspace/WorkLoom熊猫电商运营系统-销售PPT.pptx")
print("PPTX saved:", len(prs.slides.__iter__.__self__._sldIdLst), "slides")
